// initial state
const state = () => ({
  username: 'wangwei'
})

// getters
const getters = {
  /* cartProducts: (state, getters, rootState) => {
  } */
}

// actions
const actions = {
  /* example
  addProductToCart ({ state, commit }, product) {
  } */
}

// mutations
const mutations = {
  /* pushProductToCart (state, { id }) {
  }, */
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
